server:
  port: 8063

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://localhost:5443/user_db
    username: user
    password: user
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
#    properties:
#      hibernate:
#        type:
#          preferred_duration_jdbc_type: INTERVAL_SECOND
    show-sql: true